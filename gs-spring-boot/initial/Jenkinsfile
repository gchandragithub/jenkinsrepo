pipeline {
      agent any

      tools {
          jdk '1.8.0_191'
      }

      stages {
          stage('ExecuteTestScript') {
              steps {
                  // Run it!
                  sh 'bash gs-spring-boot/initial/testerworkflow.sh'
                  sh 'gs-spring-boot/initial/gradlew build && java -jar gs-spring-boot/initial/build/libs/gs-spring-boot-0.1.0.jar&'
                  }
              }
         }
}
