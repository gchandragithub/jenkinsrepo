pipeline {
    agent any

      stages {
          stage('ExecuteTestScript') {
              steps {
                  // Run it!
                  sh 'bash gs-spring-boot/initial/testerworkflow.sh'
                  sh 'bash gs-spring-boot/initial/gradlew build && java -jar build/libs/gs-spring-boot-0.1.0.jar&'
              }
         }
    }
}
