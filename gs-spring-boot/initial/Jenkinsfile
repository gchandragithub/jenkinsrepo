pipeline {
      agent any
    
      stages {
          stage('ExecuteTestScript') {
              steps {

                      sh '''#!/bin/bash
                         JENKINS_NODE_COOKIE=dontKillMe
                         echo "HELLO WORLD"
                      '''
                    }
              }
  }
}
